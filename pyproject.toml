[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "haindy"
version = "0.6.0"
description = "Autonomous AI Testing Agent with multi-agent architecture"
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
authors = [
    {name = "Federico Keegan"},
]
keywords = ["testing", "automation", "ai", "multi-agent", "desktop-automation"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Software Development :: Testing",
    "Topic :: Software Development :: Quality Assurance",
    "Environment :: Console",
    "Operating System :: MacOS",
    "Operating System :: Microsoft :: Windows",
    "Operating System :: POSIX :: Linux",
    "Topic :: Software Development :: Testing :: Acceptance",
]

dependencies = [
    "openai>=2.36.0",
    "google-genai>=2.0.1",
    "anthropic>=0.100.0",
    "cryptography>=48.0.0",
    "platformdirs>=4.9.6",
    "rich>=15.0.0",
    "pydantic>=2.13.4",
    "python-dotenv>=1.2.2",
    "pillow>=12.2.0",
    "numpy>=2.4.4",
    "aiofiles>=25.1.0",
    "jsonlines>=4.0.0",
    "jinja2>=3.1.6",
    "tiktoken>=0.12.0",
    "evdev>=1.9.3; sys_platform == 'linux'",
    "fb-idb>=1.1.7; sys_platform == 'darwin'",
    "pynput>=1.8.1; sys_platform == 'darwin' or sys_platform == 'win32'",
    "mss>=10.2.0; sys_platform == 'darwin' or sys_platform == 'win32'",
    "keyring>=25.7.0",
]

[project.optional-dependencies]
dev = [
    "pytest==9.0.3",
    "pytest-asyncio==1.3.0",
    "pytest-cov==7.1.0",
    "black==26.3.1",
    "isort==8.0.1",
    "mypy==2.0.0",
    "ruff==0.15.12",
    "pre-commit==4.6.0",
]

[project.scripts]
haindy = "haindy.main:main"

[project.urls]
Homepage = "https://github.com/Haindy/haindy"
Repository = "https://github.com/Haindy/haindy"
Issues = "https://github.com/Haindy/haindy/issues"

[tool.setuptools.packages.find]
where = ["."]
include = ["haindy*"]

[tool.setuptools.package-data]
"haindy.skills" = ["**/*.md"]
"haindy.monitoring" = ["templates/*.j2"]

[tool.black]
line-length = 88
target-version = ['py311']
include = '\.pyi?$'

[tool.isort]
profile = "black"
line_length = 88

[tool.mypy]
python_version = "3.11"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
ignore_missing_imports = true

[tool.ruff]
target-version = "py311"
line-length = 88

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # isort
    "B",   # flake8-bugbear
    "C4",  # flake8-comprehensions
    "UP",  # pyupgrade
]
ignore = [
    "E501",  # line too long
    "B008",  # do not perform function calls in argument defaults
    "C901",  # too complex
    "UP017",  # keep existing timezone.utc style until intentional modernization
    "UP041",  # keep existing asyncio.TimeoutError style
    "UP042",  # keep existing str/Enum style
]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py", "*_test.py"]
addopts = "--strict-markers --verbose"
